ktor {
  environment = test
}

application {
  profile = TEST
}

#test sftp is defined in SftpListener

postgres {
  host = "dev-pg.intern.nav.no"
  port = "5432"
  name = "sokos-ske-krav"
  username = "test"
  password = "test"
  vaultMountPath = ""
}

slackConfig {
  url = "team_best_slack_webhook_url"
}
