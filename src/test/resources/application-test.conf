ktor {
  environment = test
}

application {
  profile = TEST
  appName = "sokos-ske-krav"
  namespace = "okonomi"
}

azure {
  jwksUri = "http://localhost/jwks"
  configIssuer = "http://localhost/issuer"
  clientId = "test-client-id"
}

maskinportenClient {
  clientId = "maskinporten_client_id"
  wellKnownUrl = "maskinporten_well_known_url"
  rsaKeyString = "maskinporten_client_jwk"
  scopes = "maskinporten_scopes"
}

ske {
  skeRestUrl = ""
}

#test sftp is defined in SftpListener

postgres {
  host = "dev-pg.intern.nav.no"
  port = "5432"
  name = "sokos-ske-krav"
  username = "test"
  password = "test"
  vaultMountPath = ""
}

slack {
  url = "sokos_ske_krav_slack_webhook_url"
  slackIdProductLeader = "<@productLeaderId>"
  slackIdDomainSpecialists = "<@domainSpecialistId1>, <@domainSpecialistId2>"
  slackIdTechnicalSpecialist = "<@technicalSpecialistId>"
}

circuitBreaker {
  waitDurationInOpenState = 50
}

timer {
  useTimer = false
  schedulerIntervalPeriodInt = 4
}
