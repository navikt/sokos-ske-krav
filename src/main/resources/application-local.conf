include file("application.conf")

ktor {
  environment = local
}

application {
  profile = LOCAL
  useAuthentication = false
  basicUsername = "user"
  basicPassword = "password"
}

sftp {
  host = "10.183.32.98"
  privateKey = "privKey"
}
