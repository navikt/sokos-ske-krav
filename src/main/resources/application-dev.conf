include "application-local.conf"

ktor {
  environment = dev
}

application {
  profile = DEV
}

sftp {
  host = "b27drvl011.preprod.local"
  privateKeyFilePath = "/var/run/secrets/ske-sftp-private-key/ske-sftp-private-key"
}

postgres {
  host = "b27dbvl030.preprod.local"
  vaultMountPath = "postgresql/preprod-fss/"
}
