include file("application.conf")

ktor {
  environment = prod
}

application {
  profile = PROD
  useAuthentication = true
}

sftp {
  host = "filmottak.adeo.no"
  port = 22
  privateKeyFilePath = "/var/run/secrets/ske-sftp-private-key/ske-sftp-private-key"
}

postgres {
  host = "A01DBVL037.adeo.no"
  vaultMountPath = "postgresql/prod-fss/"
}

circuitBreaker {
  waitDurationInOpenState = 3
}
